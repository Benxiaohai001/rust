[package]
name = "gussind_game"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# rand = "0.8.5"
rand = "^0.8.5" # 表示0.8.5到0.9之间的版本都可以
